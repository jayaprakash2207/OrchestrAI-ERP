# Routes Package

HTTP routes are versioned under `app/api/v1/endpoints/`.

This folder is reserved for route grouping helpers or compatibility shims if the project later standardizes on a flatter `routes/` import surface.
