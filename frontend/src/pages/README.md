# Pages Directory

The frontend uses the Next.js App Router under `src/app/`.

This folder is kept for optional legacy `pages/` routes, API route migrations, or phased upgrades if the team needs them later.
